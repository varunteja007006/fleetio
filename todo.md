1. Driver will login
   Admin - CRUD on approve user
   Driver - once approved access app
   We need roles as well for users

2. Assign the route
   Admin - CRUD (soft delete)
   Route with checkpoints
   Admin - then assign

3. Once loaded he will start the route
   Start route

4. During the Transit the app should have multiple check points and if the driver doesn't cross the check points in the designated time,
   an alert should trigger to both driver and manager saying that this running delay

5. Then the driver has to upload photos of traffic/breakdown and select incident reason

6. The app should consider the above data and create a message and send it to mapped whatsapp group - possible
